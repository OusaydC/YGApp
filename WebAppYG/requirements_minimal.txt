# Minimal requirements for Render deployment
# Updated for Python 3.13 compatibility

# Core Django
Django==5.0.7
djangorestframework==3.15.2
django-cors-headers==4.3.1

# Production server
gunicorn==21.2.0
whitenoise==6.5.0

# Data processing (essential) - Updated for Python 3.13
pandas>=2.2.0
numpy>=1.26.0
openpyxl==3.1.2

# API filtering
django-filter==23.5

# Task queue
celery==5.3.1
django-celery-results==2.5.1
django-celery-beat==2.7.0

# Utilities
python-dateutil==2.8.2
pytz==2023.3

# Note: Excluded packages that can be installed later if needed:
# - geopandas (large, only needed for shapefile processing)
# - psycopg2-binary (only needed for PostgreSQL)
# - Pillow (only needed for image processing)
# - django-extensions (development tool)
# - django-debug-toolbar (development tool)
# - drf-spectacular (API docs, optional)
# - redis (only needed if using Redis with Celery)





